const dirs=[[1,0],[0,1],[1,1],[1,-1]], key=(x,y)=>`${x},${y}`;
export class GameEngine{constructor(){this.reset()} reset(){this.board=new Map();this.turn='X';this.winner=null} get(x,y){return this.board.get(key(x,y))} count(x,y,dx,dy,s){let c=0;for(let nx=x+dx,ny=y+dy;this.get(nx,ny)===s;nx+=dx,ny+=dy)c++;return c} win(x,y,s){return dirs.some(([dx,dy])=>1+this.count(x,y,dx,dy,s)+this.count(x,y,-dx,-dy,s)>=5)} place(x,y,s){if(this.winner||this.get(x,y))return false;this.board.set(key(x,y),s);if(this.win(x,y,s))this.winner=s;else this.turn=s==='X'?'O':'X';return true} ai(){if(this.board.size===0)return[0,0];const cand=new Set();for(const k of this.board.keys()){const [x,y]=k.split(',').map(Number);for(let dx=-1;dx<=1;dx++)for(let dy=-1;dy<=1;dy++)if(dx||dy){const nx=x+dx,ny=y+dy;if(!this.get(nx,ny))cand.add(key(nx,ny))}}const cells=[...cand].map(k=>k.split(',').map(Number));const score=(x,y,s)=>Math.max(...dirs.map(([dx,dy])=>1+this.count(x,y,dx,dy,s)+this.count(x,y,-dx,-dy,s)));let best=cells[0],bs=-1;for(const [x,y] of cells){const sc=score(x,y,'O')*2+score(x,y,'X')*2.3-0.02*(Math.abs(x)+Math.abs(y))+Math.random()*0.2;if(sc>bs){bs=sc;best=[x,y]}}return best}}
